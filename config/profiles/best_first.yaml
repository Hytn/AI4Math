# AUTO-GENERATED by ``scripts/dump_profile_yamls.py`` from
# ``prover.unified.profiles.PRESETS``. Edit this file freely —
# the next regen will overwrite it, but tests pin round-trip
# semantics so a hand-edit will not silently drift from the
# Python preset.
#
# Load:
#   from prover.unified import load_profile_from_yaml, register_profile
#   register_profile(load_profile_from_yaml("config/profiles/best_first.yaml"))
#
# Then ``--profile best_first`` becomes selectable on the CLI.
#
# Best-first search — outer driver picks the highest-scoring leaf,
# agent does single-tactic expansion. v4: search tree lands in
# ``meta.search_tree`` of dialog.json.

name: best_first
description: 'Best-first search: 外部 driver 选启发式分最高的开放叶子, 每个节点用 max_turns=1 的 agent
  expansion. v4 起进入 PRESETS, search_tree 写入 meta.search_tree.'
tools:
- tactic_apply
max_turns: 1
temperature: 0.7
model: claude-sonnet-4-20250514
framing: step_level_pure
search:
  kind: best_first
  max_nodes: 200
  max_depth: 25
  beam_width: 8
  ucb_c: 1.414
  expansion_max_turns: 1
  parallel_profiles: []
observation:
  auto_inject_lean_compile: true
  auto_inject_goal_state: true
  compress_errors_budget: 1200
  visible_history_turns: -1
  include_search_state_in_prompt: false
  include_knowledge_briefing: true
  inject_premises_in_prompt: true
  n_premises: 10
  inject_few_shot: true
  inject_similar_dialogs: false
  n_similar_dialogs: 3
  similar_dialogs_max_chars: 2000
stop:
  on_proof_found: true
  on_text_only: false
  on_all_goals_closed: true
  max_total_tokens: 200000
  timeout_seconds: 300.0
plugins: []
