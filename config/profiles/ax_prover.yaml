# AUTO-GENERATED by ``scripts/dump_profile_yamls.py`` from
# ``prover.unified.profiles.PRESETS``. Edit this file freely —
# the next regen will overwrite it, but tests pin round-trip
# semantics so a hand-edit will not silently drift from the
# Python preset.
#
# Load:
#   from prover.unified import load_profile_from_yaml, register_profile
#   register_profile(load_profile_from_yaml("config/profiles/ax_prover.yaml"))
#
# Then ``--profile ax_prover`` becomes selectable on the CLI.
#
# Ax-Prover (arXiv:2510.12787) — general-purpose LLM + Lean tools,
# Orchestrator/Prover/Verifier closed loop. Sketch → have/sorry
# skeleton → fill steps one at a time, verifying each with
# diagnostics; pass@1 = one attempt of interleaved reasoning and
# tool calls (paper budget: ≤200 calls, 25-min timeout).

name: ax_prover
description: 'Ax-Prover (arXiv:2510.12787) 复现: 通用 LLM + Lean 工具的agentic 定理证明. 工作流
  = NL proof sketch → have/sorry 骨架 → 逐步填充, 每步用诊断工具验证; Verifier 独立判定 (编译通过且无 sorry);
  Orchestrator 反馈循环直到 verified 或预算耗尽. 论文配置: Sonnet 4, ≤200 API calls, 25min timeout,
  pass@1.'
tools:
- lean_verify
- goal_inspect
- premise_search
- tactic_suggest
- lean_auto
max_turns: 200
temperature: 0.7
model: claude-sonnet-4-20250514
framing: ax_prover
search:
  kind: none
  max_nodes: 200
  max_depth: 25
  beam_width: 8
  ucb_c: 1.414
  expansion_max_turns: 1
  parallel_profiles: []
observation:
  auto_inject_lean_compile: true
  auto_inject_goal_state: false
  include_search_state_in_prompt: false
  include_knowledge_briefing: false
  inject_premises_in_prompt: false
  n_premises: 10
  inject_few_shot: false
  inject_similar_dialogs: false
  n_similar_dialogs: 3
  similar_dialogs_max_chars: 2000
stop:
  on_proof_found: true
  on_text_only: false
  on_all_goals_closed: true
  max_total_tokens: 1000000
  timeout_seconds: 1500.0
integrity_strict: false
