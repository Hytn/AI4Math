# AUTO-GENERATED by ``scripts/dump_profile_yamls.py`` from
# ``prover.unified.profiles.PRESETS``. Edit this file freely —
# the next regen will overwrite it, but tests pin round-trip
# semantics so a hand-edit will not silently drift from the
# Python preset.
#
# Load:
#   from prover.unified import load_profile_from_yaml, register_profile
#   register_profile(load_profile_from_yaml("config/profiles/dsp_v2_heterogeneous.yaml"))
#
# Then ``--profile dsp_v2_heterogeneous`` becomes selectable on the CLI.
#

name: dsp_v2_heterogeneous
description: DSP-V2 4 路异构并行 (non-CoT + CoT + repair + repair+knowledge) + broadcast
  bus. 任一路成功即整 sample 成功. 样本预算 N 实际等价于单 profile 的 pass@(N×4) 上界.
tools:
- lean_verify
- broadcast
max_turns: 4
temperature: 1.0
model: claude-sonnet-4-20250514
framing: deepseek_prover_v2_repair
search:
  kind: parallel
  max_nodes: 200
  max_depth: 25
  beam_width: 4
  ucb_c: 1.414
  expansion_max_turns: 1
  parallel_profiles:
  - dsp_v2_non_cot
  - dsp_v2_cot
  - dsp_v2_repair
  - dsp_v2_repair_knowledge
observation:
  auto_inject_lean_compile: true
  auto_inject_goal_state: false
  include_search_state_in_prompt: false
  include_knowledge_briefing: true
  inject_premises_in_prompt: true
  n_premises: 10
  inject_few_shot: true
  inject_similar_dialogs: false
  n_similar_dialogs: 3
  similar_dialogs_max_chars: 2000
stop:
  on_proof_found: true
  on_text_only: false
  on_all_goals_closed: true
  max_total_tokens: 200000
  timeout_seconds: 300.0
integrity_strict: false
