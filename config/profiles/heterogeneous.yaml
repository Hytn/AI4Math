# AUTO-GENERATED by ``scripts/dump_profile_yamls.py`` from
# ``prover.unified.profiles.PRESETS``. Edit this file freely —
# the next regen will overwrite it, but tests pin round-trip
# semantics so a hand-edit will not silently drift from the
# Python preset.
#
# Load:
#   from prover.unified import load_profile_from_yaml, register_profile
#   register_profile(load_profile_from_yaml("config/profiles/heterogeneous.yaml"))
#
# Then ``--profile heterogeneous`` becomes selectable on the CLI.
#
# AI4Math flagship — N parallel sub-profiles + broadcast bus.
# Each direction explores under a different framing / tools /
# turn budget; a shared BroadcastBus shares discoveries.

name: heterogeneous
description: N 个不同 framing/model/temp 的 sub-profile 并行 + 广播总线
tools:
- lean_verify
- broadcast
max_turns: 4
temperature: 0.7
model: claude-sonnet-4-20250514
framing: whole_proof_repair
search:
  kind: parallel
  max_nodes: 200
  max_depth: 25
  beam_width: 4
  ucb_c: 1.414
  expansion_max_turns: 1
  parallel_profiles:
  - whole_proof
  - reprover
  - leandojo
  - whole_proof_repair
observation:
  auto_inject_lean_compile: true
  auto_inject_goal_state: false
  compress_errors_budget: 1200
  visible_history_turns: -1
  include_search_state_in_prompt: false
  include_knowledge_briefing: true
  inject_premises_in_prompt: true
  n_premises: 10
  inject_few_shot: true
  inject_similar_dialogs: false
  n_similar_dialogs: 3
  similar_dialogs_max_chars: 2000
stop:
  on_proof_found: true
  on_text_only: false
  on_all_goals_closed: true
  max_total_tokens: 200000
  timeout_seconds: 300.0
plugins: []
