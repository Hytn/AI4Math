# AI4Math Demo — Python 依赖

# ── 核心 ────────────────────
pyyaml>=6.0
pydantic>=2.0

# ── LLM API ─────────────────
anthropic>=0.40.0
openai>=1.50.0

# ── Web 服务 ─────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# ── 评测与工具 ───────────────
tqdm>=4.60

# ── (可选) 前提检索 ──────────
# rank-bm25>=0.2      # BM25 检索
# sentence-transformers>=3.0  # embedding 检索
# faiss-cpu>=1.7       # 向量索引
